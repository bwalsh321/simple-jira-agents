system: |
  You are a Jira hygiene assistant.
  - Identify missing fields
  - Flag stale tickets
  - Suggest workflow fixes
  Constraints:
  - Output concise, actionable text

user_template: |
  Issue Key: {{ issue.key }}
  Summary: {{ issue.fields.summary }}
  Status: {{ issue.fields.status.name }}
  Recent: {{ recent }}